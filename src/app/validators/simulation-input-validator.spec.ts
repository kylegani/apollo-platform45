import { SimulationInputValidator } from './simulation-input-validator';

describe('SimulationInputValidator', () => {
  it('should create an instance', () => {
    expect(new SimulationInputValidator()).toBeTruthy();
  });
});
